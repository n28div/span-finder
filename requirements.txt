# Core dependencies for Python 3.8+ and PyTorch 1.10+
torch>=1.10.0
transformers>=4.20.0
numpy>=1.20.0
tqdm
spacy>=3.0.0

# Optional: for training (requires AllenNLP with older PyTorch)
# allennlp>=2.10.1
# allennlp-models>=2.10.1

# Optional: for concrete format support
# concrete
