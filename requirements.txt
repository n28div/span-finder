allennlp==2.9.3
transformers==4.18.0
numpy==1.21.0
scipy
tqdm
cached-path==1.1.2
charset-normalizer==3.3.2
huggingface_hub>=0.9.0

# GPU build of PyTorch — replace with the CPU wheel if no CUDA is available
--extra-index-url https://download.pytorch.org/whl/cu113
torch==1.11.0+cu113
